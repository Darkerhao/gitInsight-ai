# Review Pass 1

## Reviewer

### Findings

1. P0：内容工时分配在项目数超过 0.5 小时单位数时会超过每日默认总工时。
2. P1：默认日期范围实现为本周一至周日，偏离设计的最近 7 个自然日。
3. P1：生成期间日期和项目范围仍可修改，异步结果可能写回已经脱离当前页面的草稿。
4. P1：核心页面工作流只有纯函数测试，没有生成/保存/提交编排的静态或组件契约覆盖。
5. P0：`WeeklyReportsView.vue` 同时承担状态编排、IPC 和 UI，文件职责过重。

### Pass 1 修复

- 工时算法在项目数超过每日可用 0.5 小时单位时停止自动分配，页面提示手动设置，并补充边界测试。
- 默认范围改为今天向前 6 天。
- 生成/提交期间禁用日期与项目范围。
- 新增 `weeklyReportPage.smoke.mjs`，覆盖菜单/路由、历史覆盖保存、脏草稿提交前保存、顺序批量提交、成功项跳过与范围锁定。
- 将周日报草稿状态与 IPC 编排移入 `useWeeklyReports.ts`，页面只保留展示和事件绑定。

## Spec Compliance

- MISSING：无。
- EXTRA：无。
- DEVIATED：默认日期范围不是最近 7 个自然日。
- 初始总评：REWORK；上述偏差已在 Pass 1 后修复。
